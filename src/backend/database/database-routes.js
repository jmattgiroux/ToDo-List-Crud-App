// entry route for the app

const router = require("express").Router();

// router.use("/user", require("./user-features/user-routes"));
// router.use("/api-docs", require("./user-features/user-routes"));

module.exports = router;